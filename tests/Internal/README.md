# Internal Tests

Mirrors `src/Internal` for tests that validate Internal compiler, tooling, CLI, editor, language server, and runtime behavior.

Keep host-specific integration tests under `tests/ExternalSupport`.
