# Cli

Owns tests for Internal CLI command routing, help text, command output, and command-specific adapters.

Compiler semantics should be asserted in `Compiler` unless the command contract itself is under test.
