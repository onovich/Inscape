# WorkspaceIndex

Transitional directory.

`DslScript` authoring providers have moved to `../DslScript`. This directory currently only holds Host Bridge / Host Schema and editor-authoring data providers until the next pass moves them under business directories.

Do not add new files here.
