{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Inscape Host Schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "format",
    "formatVersion"
  ],
  "properties": {
    "format": {
      "const": "inscape.host-schema",
      "description": "Identifies this file as an Inscape host capability schema."
    },
    "formatVersion": {
      "type": "integer",
      "const": 1,
      "description": "Host schema format version."
    },
    "queries": {
      "type": "array",
      "description": "Pure host queries that DSL interpolation and condition expressions may reference.",
      "items": {
        "$ref": "#/$defs/query"
      },
      "default": []
    },
    "actions": {
      "type": "array",
      "description": "Host actions that @ syntax may trigger. New P3 capabilities should use actions[] instead of legacy events[].",
      "items": {
        "$ref": "#/$defs/action"
      },
      "default": []
    },
    "events": {
      "type": "array",
      "description": "Legacy host events or callbacks. Kept for compatibility; prefer actions[] for new P3 host capabilities.",
      "deprecated": true,
      "items": {
        "$ref": "#/$defs/event"
      },
      "default": []
    }
  },
  "$defs": {
    "identifier": {
      "type": "string",
      "pattern": "^[A-Za-z_][A-Za-z0-9_\\-.]*$"
    },
    "typeName": {
      "type": "string",
      "enum": [
        "bool",
        "int",
        "float",
        "number",
        "string",
        "asset",
        "void"
      ]
    },
    "parameter": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "type"
      ],
      "properties": {
        "name": {
          "$ref": "#/$defs/identifier",
          "description": "Parameter name."
        },
        "type": {
          "$ref": "#/$defs/typeName",
          "description": "Host-neutral parameter type."
        },
        "idKind": {
          "$ref": "#/$defs/identifier",
          "description": "Optional Inscape-side id kind for string parameters, such as item, timeline, or speaker."
        },
        "required": {
          "type": "boolean",
          "default": true
        },
        "description": {
          "type": "string"
        }
      }
    },
    "query": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "parameters",
        "returnType"
      ],
      "properties": {
        "name": {
          "$ref": "#/$defs/identifier",
          "description": "Query name referenced by future expression syntax."
        },
        "description": {
          "type": "string"
        },
        "returnType": {
          "$ref": "#/$defs/typeName",
          "description": "Query return type. Queries should not use void."
        },
        "idKind": {
          "$ref": "#/$defs/identifier",
          "description": "Optional Inscape-side id kind when the query itself returns a readable id."
        },
        "isAsync": {
          "type": "boolean",
          "default": false,
          "description": "Whether the host may need asynchronous evaluation."
        },
        "parameters": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/parameter"
          },
          "default": []
        }
      }
    },
    "action": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "parameters",
        "mode"
      ],
      "properties": {
        "name": {
          "$ref": "#/$defs/identifier",
          "description": "Action name referenced by @ syntax or future action expressions."
        },
        "description": {
          "type": "string"
        },
        "mode": {
          "type": "string",
          "enum": [
            "fire",
            "wait",
            "handoff"
          ],
          "description": "Action execution mode. fire does not block narrative flow, wait resumes after host completion, and handoff transfers control to the host until it resumes Inscape."
        },
        "idKind": {
          "$ref": "#/$defs/identifier",
          "description": "Optional Inscape-side id kind when the action name is also a readable id category."
        },
        "parameters": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/parameter"
          },
          "default": []
        }
      }
    },
    "event": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "$ref": "#/$defs/identifier",
          "description": "Event or callback name referenced by future event syntax."
        },
        "description": {
          "type": "string"
        },
        "delivery": {
          "type": "string",
          "enum": [
            "fire-and-forget",
            "blocking",
            "queued"
          ],
          "default": "fire-and-forget"
        },
        "sideEffects": {
          "type": "boolean",
          "default": true,
          "description": "Events are allowed to mutate host state; queries are not."
        },
        "parameters": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/parameter"
          },
          "default": []
        }
      }
    }
  }
}
