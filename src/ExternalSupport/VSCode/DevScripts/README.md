# DevScripts

Owns VSCode package-local development scripts, such as `.vsix` rebuild, local install helpers, repeatable manual smoke launchers, and package structure contract checks.

These scripts are excluded from the packaged extension unless the package manifest explicitly needs them at runtime.
