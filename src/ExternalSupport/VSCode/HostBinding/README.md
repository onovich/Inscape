# HostBinding

Owns VSCode authoring providers for Host Bridge binding rows and timed host resource hooks such as `@timeline`.

- `Providers` contains Host Bridge lookup, completion, definition, hover, and lightweight workspace occurrence behavior.

Host Bridge contract design belongs in repository docs and Internal tooling contracts. This directory adapts the current contracts to VSCode authoring behavior.
