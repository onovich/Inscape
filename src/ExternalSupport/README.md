# ExternalSupport

Holds host-specific, integration-specific, or prototype support code outside the Internal product layers.

Allowed layers: `VSCode`, `UnityPlugin`, and future host or platform support packages.

Do not place compiler truth, shared toolchain use cases, or editor-neutral contracts here.
