# Commands

Owns UnitySample command implementations for export templates, project export, and L10N merge.

These commands may adapt Internal `Tooling` results to UnitySample structures, but must not move Unity-specific assumptions into Internal layers.
