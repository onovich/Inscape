# AssetConfigure

Owns Unity asset configuration flows, including Addressables-facing experiments when needed.

Allowed roles: `Controllers` and `Models`.

Do not make asset configuration assumptions visible to Internal layers.
