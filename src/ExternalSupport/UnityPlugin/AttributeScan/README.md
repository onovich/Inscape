# AttributeScan

Owns Unity `[Inscape]` attribute scanning experiments and generated candidate bridge table models.

Allowed roles: `Controllers` and `Models`.

Do not require this prototype from the compiler or shared tooling layers.
