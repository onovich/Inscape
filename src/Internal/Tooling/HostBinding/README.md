# HostBinding

Owns reusable host binding table reading, validation, and host binding view models.

Allowed roles: `Domains`, `Controllers`, `Models`, and `ViewModels`.

Do not hard-code UnitySample, Addressables, ScriptableObject, or Bird-specific structures here.
