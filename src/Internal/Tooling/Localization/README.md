# Localization

Owns tool-level localization flows such as CSV reading, extraction orchestration, update reports, and reusable localization view models.

Allowed roles: `Domains`, `Models`, and `ViewModels`.

Do not include host-specific localization merge formats or editor command wiring.
