# ProjectSources

Owns project source discovery, source loading, exclusions, overrides, and source set models.

Allowed roles: `Domains` and `Models`.

Do not perform command-line parsing or editor-specific workspace indexing here.
