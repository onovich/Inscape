# LanguageServer

Reserved for the future C# language server that will centralize editor semantics for diagnostics, definitions, references, completion, outline, and hover.

Allowed business areas: `ServerEntry`, `DslScript`, `StoryGraph`, and `HostSchema`.

Do not create a broad project service here before narrower Compiler and Tooling contracts are stable.
