# HostSchema

Reserved for language server providers and controllers for host schema authoring support.

Allowed roles: `Providers`, `Controllers`, and `Models`.

Do not bind to a concrete host project.
