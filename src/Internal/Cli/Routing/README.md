# Routing

Owns top-level command dispatch, command metadata, and CLI route selection.

Allowed roles: `Commands`, `Models`, and `ViewModels`.

Do not include compiler logic or reusable toolchain use cases.
