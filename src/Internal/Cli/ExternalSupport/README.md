# ExternalSupport

Owns CLI command adapters that invoke external support prototypes while keeping the public CLI entry point stable.

Allowed roles: `Commands`.

Do not let external host assumptions leak into compiler or shared tooling semantics.
