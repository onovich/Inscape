# HostBinding

Owns CLI commands for host binding-oriented workflows.

Allowed roles: `Commands`.

Do not hard-code external adapter structures unless the command is explicitly under `ExternalSupport`.
