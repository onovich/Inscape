# EditorAuthoring

Owns VSCode authoring controllers, bridges, and view models outside preview playback.

Allowed roles: `Bridges`, `Controllers`, and `ViewModels`.

Do not include shared editor-neutral contracts that should live in `Tooling/EditorAuthoring`.
