# PreviewWebview

Owns VSCode preview webview commands, bridges, controllers, and view models.

Allowed roles: `Bridges`, `Commands`, `Controllers`, and `ViewModels`.

Do not place reusable preview rendering logic here when it can live in `Tooling/Preview`.
