# LanguageFeatures

Owns VSCode language feature providers such as completion, definition, reference, hover, diagnostics bridge, outline, and CodeLens.

Allowed business areas: `DslScript`, `StoryGraph`, `HostSchema`, `HostBinding`, and `Metadata`.

Do not place preview webview lifecycle or extension activation routing here.
