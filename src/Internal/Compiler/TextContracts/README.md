# TextContracts

Owns shared text normalization and source text contracts used by compiler-facing features.

Allowed roles: `Domains` and `Models`.

Do not include editor style configuration, preview rendering, or external asset metadata.
