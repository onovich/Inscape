# Diagnostics

Owns compiler diagnostic models, severities, and diagnostic construction rules.

Allowed roles: `Domains` and `Models`.

Do not include editor decorations, terminal formatting, or host import warnings.
