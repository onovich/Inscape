# Localization

Owns compiler-level localization anchors, extractable text identity, CSV-neutral localization models, and merge semantics that depend on compiler output.

Allowed roles: `Domains` and `Models`.

Do not include CLI file routing, VSCode commands, or host-specific localization formats.
