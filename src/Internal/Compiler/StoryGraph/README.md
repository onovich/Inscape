# StoryGraph

Owns narrative graph compilation, edges, entry selection, reachability, project graph validation, and IR-facing graph models.

Allowed roles: `Domains` and `Models`.

Do not include preview UI, command output formatting, or host-specific export structures.
