# DslScript

Owns source-level DSL parsing rules, node declarations, line syntax, metadata syntax, and source spans.

Allowed roles: `Domains` and `Models`.

Do not include CLI argument parsing, VSCode provider behavior, host binding resolution, or runtime execution state.
