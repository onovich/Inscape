# StoryRuntime

Reserved for runtime story execution systems, contexts, events, and models.

Allowed roles: `Systems`, `Contexts`, `Events`, and `Models`.

Do not create runtime loop behavior before the runtime phase is designed.
